{
  "compilerOptions": {
        "strict": true,
        "target": "ES2020",        // <-- language standard (JS features available)
        "module": "ESNext",        // <-- module format (import/export behavior)
        "lib": ["ES2020", "DOM"]   // <-- built-in APIs available
  }
}